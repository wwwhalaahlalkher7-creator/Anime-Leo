# V1.17.1 Fix

The production API was verified as healthy:
- `/api/health` returns HTTP 200.
- D1 is connected.
- The catalog contains 16 anime.
- `/api/top/anime` returns HTTP 200 with anime data.

The mobile release build was therefore fixed at the Android network layer:
1. Explicitly add `android.permission.INTERNET` after the generated Android platform files are created.
2. Prevent local monitoring/SharedPreferences failures from changing successful API calls into failures.
3. Improve the health-check error message for network failures.

Rebuild the APK through the included GitHub Actions workflow.
